services:
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - base-site
    ports:
      - "80:3000"
  base-site:
    build: ./NextApp
    deploy:
      replicas: 10
    # ports:
    #   - "80:3000"
  game-logic:
    build: ./SocketIOApp
    ports:
      - "8080:8080"
